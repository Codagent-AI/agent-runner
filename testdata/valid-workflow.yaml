name: test-workflow
description: "A test workflow"
params:
  - name: project
    required: true
  - name: env
    required: false
    default: staging
steps:
  - id: setup
    command: echo "setting up {{project}}"
  - id: deploy
    agent: headless_base
    mode: headless
    prompt: "Deploy {{project}} to {{env}}"
