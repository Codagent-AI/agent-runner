name: finalize-repositories
description: "Finalize one pull request per selected implementation repository."
hidden: true
scope: repositories

params:
  - name: repositories
    required: true

steps:
  - id: finalize-repository
    workflow: ../core/finalize-pr-v1.0.yaml
