name: change
description: "Full plan + implement + validate + PR creation workflow. Use for normal feature development."

sessions:
  - name: lead-agent
    agent: planner

steps:
  - id: plan
    workflow: plan-change.yaml

  - id: implement
    workflow: implement-change.yaml
