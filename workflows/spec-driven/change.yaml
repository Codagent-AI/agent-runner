name: change
description: "Full spec-driven planning "

sessions:
  - name: lead-agent
    agent: planner

steps:
  - id: plan
    workflow: plan-change.yaml

  - id: implement
    workflow: implement-change.yaml
