name: change
description: "Full spec-driven plan + implement + PR creation workflow"

sessions:
  - name: lead-agent
    agent: planner

steps:
  - id: plan
    workflow: plan-change.yaml

  - id: implement
    workflow: implement-change.yaml
