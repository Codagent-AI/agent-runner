name: smoke-test
description: "Quick smoke test for interactive step completion signaling"

steps:
  - id: greet
    agent: interactive_base
    prompt: |
      Say hello.
      Once you've done that, continue to the next step.

  - id: how-are-you
    session: resume
    prompt: |
      Ask the user how they are doing.
