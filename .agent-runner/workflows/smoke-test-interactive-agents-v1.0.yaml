name: smoke-test-interactive-agents
description: "Manual smoke test for interactive agent adapters"

steps:
  - id: claude-interactive
    agent: claude_interactive_smoke
    session: new
    prompt: "Reply with exactly this short sentence: claude interactive smoke ready. Then wait for me to continue."

  - id: claude-interactive-resume
    session: resume
    prompt: "Confirm you remember saying claude interactive smoke ready, then reply with exactly this short sentence: claude interactive resume ready. Then wait for me to continue."

  - id: codex-interactive
    agent: codex_interactive_smoke
    session: new
    prompt: "Reply with exactly this short sentence: codex interactive smoke ready. Then wait for me to continue."

  - id: codex-interactive-resume
    session: resume
    prompt: "Confirm you remember saying codex interactive smoke ready, then reply with exactly this short sentence: codex interactive resume ready. Then wait for me to continue."

  - id: copilot-interactive
    agent: copilot_interactive_smoke
    session: new
    prompt: "Reply with exactly this short sentence: copilot interactive smoke ready. Then wait for me to continue."

  - id: copilot-interactive-resume
    session: resume
    prompt: "Confirm you remember saying copilot interactive smoke ready, then reply with exactly this short sentence: copilot interactive resume ready. Then wait for me to continue."

  - id: cursor-interactive
    agent: cursor_interactive_smoke
    session: new
    prompt: "Reply with exactly this short sentence: cursor interactive smoke ready. Then wait for me to continue."

  - id: cursor-interactive-resume
    session: resume
    prompt: "Confirm you remember saying cursor interactive smoke ready, then reply with exactly this short sentence: cursor interactive resume ready. Then wait for me to continue."

  - id: opencode-interactive
    agent: opencode_interactive_smoke
    session: new
    prompt: "Reply with exactly this short sentence: opencode interactive smoke ready. Then wait for me to continue."

  - id: opencode-interactive-resume
    session: resume
    prompt: "Confirm you remember saying opencode interactive smoke ready, then reply with exactly this short sentence: opencode interactive resume ready. Then wait for me to continue."
