profiles:
    headless_base:
        default_mode: headless
        cli: claude
        model: sonnet
        effort: high
    implementor:
        extends: headless_base
    haiku_tester:
        extends: headless_base
        model: haiku
    interactive_base:
        default_mode: interactive
        cli: claude
        model: opus
        effort: high
    planner:
        extends: interactive_base
